# Phase 3 Verification Report — Mechanic Library & Deterministic Physics

**Date:** 2026-07-09 · **Session:** 3 · **Suite:** 186/186 green (`npm test`) · **PKG:** `s3-b4d8e6`

P3's exit gate (IRD): *"single-jump invariant holds under fuzzed input (never >1 jump); each component has isolated tests; deterministic trajectory reproducible."* This report maps every P3 REQ to a named passing test (dm-0008 per-REQ rule), records the mandatory Subtractive Removal pass, and closes **milestone M1 — Simulatable Game**.

The P3 section was authored before any P3 code via an adversarial review of the original slice table (decisions dm-0016–dm-0021), which added the run-lifecycle slice (S3.4), fixed input-system ownership, and upgraded the collision/kinematics/jump-lock techniques below.

---

## 1. Slice → deliverable → proof map

| Slice | Deliverable | Proving tests |
|---|---|---|
| S3.1 | `src/components/{Tuning,CollisionClass}.ts`; `WorldState` physics fields; `src/systems/PlayerPhysics.ts` (symplectic Euler, swept axis-separated resolution) | `PlayerPhysics.test.ts` (16: velocity-first integration, fall clamp, exact floor/ceiling/wall snapping, **300-case seeded no-tunneling fuzz at 1200 u/s**, entity solidity incl. door open/closed + spike pass-through + stand-on-entity, classification totality, trajectory determinism, purity) + `PhysicsReplay.test.ts` (3: fixture-file bit-identical replay, exact floor rest, level reference-shared) |
| S3.2 | `src/systems/SpatialPartition.ts` — deterministic quadtree + physics broad-phase wiring | `SpatialPartition.test.ts` (7: **fuzz equivalence-vs-brute-force** 40 rounds × 25 queries, structural determinism, neighborhood-only, flush convention, 300-entity cluster depth bound, purity, empty) |
| S3.3 | `src/systems/PlayerControl.ts` — sole input consumer, instant accel/decel | `PlayerControl.test.ts` (6: axis→velocity mapping, instant stop/reversal, vertical untouched, purity/no-op identity, walk-to-wall pipeline replay, exact data-driven step distance) |
| S3.4 | `src/systems/{Lifecycle,HazardsAndGoal}.ts` — goal / defeat / instant reload | `Lifecycle.test.ts` (6: goal completes/clear does not, completion freeze until reset, pit-fall defeat + next-tick fresh world equals a re-derived instantiation, resetPressed reload, cross-reload replay determinism, purity) |
| S3.5 | Single-jump lock machine (the axiom) in `PlayerControl` + `WorldState.jumpLock` | `JumpLock.test.ts` (8: anticipation timing + exact impulse, air-press ignored-not-consumed, no-restart/no-stack, committed anticipation survives support loss, spent-is-forever, horizontal-still-works-while-spent, reload re-arms, **THE PROPERTY: 40 seeded 300-tick fuzz tapes across defeat/reset → never >1 impulse per life, never a backward transition**) |
| S3.6 | `src/systems/EntityKinematics.ts` (closed-form movers + collapse); platform carry + ice | `Environmental.test.ts` (10: looping-wrap + linear-ping-pong period exactness, dormant triggered mover, horizontal carry tracks exactly, upward-platform grounding robust, collapse latch+delay+fall-through, ice momentum + iceAccel·dt ramp, full-pipeline replay, purity) |
| S3.7 | Lethal hazards (swept) in `HazardsAndGoal`; `src/systems/Sensors.ts` (triggers) | `HazardsTriggers.test.ts` (9: spike swept pass-through, laser on/off-phase timing, moving-hazard defeat, plate→openDoor non-solid + once-latch, toggle rising-edge-not-flicker, activatePlatform + collapseFloor, **3-mechanic layering**, pipeline replay, purity) |
| S3.8 | Kinetic modifiers folded into `PlayerControl` (spring/conveyor) + `PlayerPhysics` (gravity zones) | `KineticModifiers.test.ts` (6: spring upward launch, gravity-zone inversion rises, conveyor composes with walking, **THE INVARIANT: jumpLock bit-identical across every modifier × 60 ticks + one-jump-retained**, spring-then-still-jump-once, all-modifier pipeline replay) |
| S3.9 | This report; per-REQ audit; subtractive pass; M1 close | — |

## 2. Exit-gate evidence

- **Single-jump invariant under fuzzed input:** `JumpLock.test.ts` "THE AXIOM" property — 40 seeded 300-tick random input tapes crossing natural defeats and resets, asserting never more than one impulse per life and a strictly forward-only machine. Reinforced by `KineticModifiers.test.ts` "THE INVARIANT" (no spring/zone/conveyor consumes the jump).
- **Each component isolated:** every §16 element has its own passing suite (platforms/collapse/ice in `Environmental`, spikes/lasers/hazards/plates/zones/doors in `HazardsTriggers`, springs/zones/conveyors in `KineticModifiers`), plus the physics/quadtree/controller/lifecycle cores.
- **Deterministic trajectory reproducible:** every slice carries a bit-identical replay assertion; `PhysicsReplay` drives the fixture file through the engine twice to an identical serialized world.
- **Playable start→finish:** the pipeline spawns, runs, defeats+reloads, and completes on goal overlap — a data-defined level is simulatable end-to-end.

## 3. Per-REQ verdicts (dm-0008: a REQ flips VERIFIED only when its OWN backlog Phase-column scope is fully covered)

| REQ | Backlog Phase | Verdict | Rationale |
|---|---|---|---|
| REQ-004 | P3 | **VERIFIED** | The one-jump structural constraint is the `JumpLock` machine; the fuzz property proves it holds under adversarial input. |
| REQ-010 | P3 | **VERIFIED** | Exactly one jump per level, refreshed only by scene reload (pure re-instantiation, dm-0018) — proven by the reload-re-arms + property tests. |
| REQ-011 | P3 | **VERIFIED** | No upgrade/double/wall-jump/refresh path exists: the machine is forward-only (available→anticipating→spent), air presses are ignored-not-consumed, and no code exits `spent` within a life (dm-0020). |
| REQ-151 | P3 | **VERIFIED** | Static geometry (tiles), moving platforms (linear/looping/triggered) with carry, collapsing floors, frictionless ice — all data-driven and isolated-tested (`Environmental`). |
| REQ-152 | P3 | **VERIFIED** | Spikes, timed lasers, moving hazards (swept lethal → defeat); pressure plates, proximity zones, mechanical doors modifying layout via the closed trigger-action union (`HazardsTriggers`). |
| REQ-153 | P3 | **VERIFIED** | Springs, gravity-inverting zones, conveyors alter velocity/inertia; the property test proves none consume the single jump (`KineticModifiers`). |
| REQ-154 | P3 | **VERIFIED** | Components are decoupled (systems key on the `CollisionClass` data table + component presence, never entity identity); the layering test composes plate→door + platform into emergent behavior from data wiring alone. |
| REQ-150 | P3,P9 | IN_PROGRESS | The P3 simulation share is done and tested (instant accel/decel, anticipation frames, the horizontal-only lock); the P9 visual clauses (squash-and-stretch, particle burst, camera tracking) are unbuilt. Correctly stays open (dm-0019 re-scope). |
| REQ-003 | P3,P10 | IN_PROGRESS | P3 delivers continuous kinetic momentum + instant iteration (defeat→one-tick reload); the P10 share (content pacing) is content-gated behind M2. |
| REQ-160 | P3,P11 | IN_PROGRESS | P3 holds the structural-preallocation stance (fixed-size tile scans, no per-tile closures); the zero-allocation runtime audit is P11's (dm-0004 defers pooling). |
| REQ-162 | P3,P9,P11 | IN_PROGRESS | The spatial-partition (quadtree neighborhood collision) half is delivered and fuzz-verified; render batching (P9) and the perf audit (P11) remain. |
| REQ-P02 | P0–P11 | IN_PROGRESS | Cross-cutting; satisfied *for P3* (plan authored before code via adversarial review; per-slice SDLC loop archived in `meta/runs/S3.*`), perpetual by design. |

Backlog rollup after flips: **40 NOT_STARTED / 14 IN_PROGRESS / 0 COMPLETED / 13 VERIFIED.**

## 4. Subtractive Removal pass (REQ-022, P3-scoped modules)

Sweep of `src/systems/*` and the S3 additions to `src/components/*` + `src/entities/World.ts`:

- **0 findings to remove.** Every S3 export (`stepPlayerPhysics`, `playerPhysicsSystem`, `buildEntityQuadtree`, `queryQuadtree`, `playerControlSystem`, `lifecycleSystem`, `hazardsAndGoalSystem`, `entityKinematicsSystem`, `sensorsSystem`, `COLLISION_CLASS_BY_KIND`, `TUNING`) has a non-defining consumer; every added `WorldState`/`EntityState` field and every `TUNING` field is read by a system.
- 0 `TODO`/`FIXME`/`console.*` markers introduced.
- No duplicated logic: the pipeline is single-responsibility per system; kinetic modifiers were folded into their integrating systems (dm-0021) precisely to avoid a redundant post-physics pass.

## 5. Invariant compliance notes

- **Determinism (dm-0017 audit):** grep of `src/systems/` confirms **no transcendental function** (`Math.sin/cos/tan/pow/exp/hypot/log/atan/…`, `Math.random`) — only IEEE-exact `+ − × ÷ %`, `Math.sqrt/min/max/abs/floor/ceil`. Waypoint traversal uses arc-length arithmetic; no delta-time scaling; every mechanic derives from the fixed-step tick.
- **No tunneling (dm-0017):** the swept axis-separated resolution is fuzz-proven at 1200 u/s against one-tile geometry; the swept lethal check (segment vs Minkowski AABB) prevents hazard skips.
- **Single source of truth / immutability:** all mutable state lives in `WorldState`; every system is a pure `step(state)=>state`; pipeline replays run bit-identically (a mutation would diverge them).
- **The axiom lives in code, not data (dm-0011/0018):** no `maxJumps`-like field exists anywhere; the lock is engine logic, refreshed only by reload's pure re-instantiation.
- **Data-driven:** no gameplay numeral is a literal in system code — feel constants come from `TUNING` (the one normative record), mechanic values from the level payload, solidity/lethality from the `CollisionClass` table.
- **Content gate:** no campaign content generated; all fixtures are unit scaffolding. The M2 hard gate is untouched.

## 6. Carried-forward ownership

- **Save/progress persistence** (P2 open question, required to resolve before M1 close): assigned to **P9 under REQ-171** (SDK lifecycle hooks own session persistence). Recorded here and in the P3 execution plan.
- **Replay-tape serialization:** remains P4's, to be assigned at P4 planning (unchanged).
- **GDOS block semantics** (difficulty axes, creator-moment frame): remain provisional until P5 (dm-0012).

## 7. Phase & milestone state

**P3 — VERIFIED.** With **P2 ✓ + P3 ✓, milestone M1 — Simulatable Game is CLOSED:** a data-defined level is playable and physically deterministic with the one-jump lock. Next: **M2 — Design Intelligence Operational** (P4 Evaluation & Validation → P5 GDOS Scoring → P6 Campaign Intelligence). The content-generation hard gate remains closed until M2 is VERIFIED.
